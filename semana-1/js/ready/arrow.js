//Arrow

const multiplicacao = function (a, b) {
    return a * b;
};

const dividir = (a, b) => {
    return a / b;
};

const objeto = () => ({nome: 'MB', sombrenome: 'Brito'});

console.log(objeto().nome)